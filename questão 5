#include <stdio.h>

void verificacao (int num) {

     if (num> = 1)

            printf ("VERDADEIRO");

        else if (num <= -1)

            printf ("FALSO");

        outro{

            printf ("NULO (0)");

            printf ("\ nSELECIONE UMA DAS OPCOES \ n \ n");

            printf ("> QUE 0 (zero) \ n <QUE 0 (zero) \ n \ n");

            a Principal();
        }
}
int main () {

    int num;

    printf ("NUMERO:");
    scanf ("% d", & num);

    verificacao (num);

    return 0;
}
